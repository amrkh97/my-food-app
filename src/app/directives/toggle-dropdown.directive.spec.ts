import { ToggleDropdownDirective } from './toggle-dropdown.directive';

describe('ToggleDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
